import { notFound } from "next/navigation"; import { CategoryForm } from "../../../../../features/admin/components/entity-forms"; import { updateCategory } from "../../../../../features/admin/actions/admin.actions"; import { getAdminCategory } from "../../../../../features/admin/data/admin-loaders";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params; const category=await getAdminCategory(id); if(!category)notFound(); return <div><h1 className="mb-5 text-3xl font-black">Edit Category</h1><CategoryForm category={category} action={async(formData)=>{"use server"; await updateCategory(id,formData)}}/></div>}
