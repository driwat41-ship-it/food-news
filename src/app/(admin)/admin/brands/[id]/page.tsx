import { notFound } from "next/navigation"; import { BrandForm } from "../../../../../features/admin/components/entity-forms"; import { updateBrand } from "../../../../../features/admin/actions/admin.actions"; import { getAdminBrand } from "../../../../../features/admin/data/admin-loaders";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params; const brand=await getAdminBrand(id); if(!brand)notFound(); return <div><h1 className="mb-5 text-3xl font-black">Edit Brand</h1><BrandForm brand={brand} action={async(formData)=>{"use server"; await updateBrand(id,formData)}}/></div>}
