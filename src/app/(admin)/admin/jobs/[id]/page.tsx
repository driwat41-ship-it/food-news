import { notFound } from "next/navigation"; import { AdminTable,Td } from "../../../../../features/admin/components/admin-table"; import { getAdminJob } from "../../../../../features/admin/data/admin-loaders";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params; const job=await getAdminJob(id); if(!job)notFound(); return <div><h1 className="mb-5 text-3xl font-black">{job.name}</h1><AdminTable headers={["Status","Attempt","Source","Duration","Records","Error"]}>{job.executions.map((x)=><tr key={x.id}><Td>{x.status}</Td><Td>{x.attempt}</Td><Td>{x.source?.name}</Td><Td>{x.durationMs}</Td><Td>{x.recordsRead}/{x.recordsWritten}</Td><Td>{x.errorMessage}</Td></tr>)}</AdminTable></div>}
