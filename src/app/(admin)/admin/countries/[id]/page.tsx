import { notFound } from "next/navigation"; import { CountryForm } from "../../../../../features/admin/components/entity-forms"; import { updateCountry } from "../../../../../features/admin/actions/admin.actions"; import { getAdminCountry } from "../../../../../features/admin/data/admin-loaders";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params; const country=await getAdminCountry(id); if(!country)notFound(); return <div><h1 className="mb-5 text-3xl font-black">Edit Country</h1><CountryForm country={country} action={async(formData)=>{"use server"; await updateCountry(id,formData)}}/></div>}
